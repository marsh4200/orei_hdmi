{
  "config": {
    "step": {
      "user": {
        "title": "OREI HDMI Matrix",
        "description": "Enter the matrix connection details. The model and input/output counts are detected automatically.",
        "data": {
          "host": "IP address",
          "port": "TCP port (default 8000; some models use 23)"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not connect to the matrix. Check the IP address and port."
    },
    "abort": {
      "already_configured": "This matrix is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "OREI HDMI Matrix options",
        "menu_options": {
          "names": "Name inputs & outputs",
          "settings": "Polling & entities"
        }
      },
      "names": {
        "title": "Friendly names",
        "description": "Leave a field blank to fall back to 'Input N' / 'Output N'."
      },
      "settings": {
        "title": "Polling & entities",
        "data": {
          "scan_interval": "Polling interval (seconds)",
          "enable_media_player": "Create media player per zone",
          "enable_select": "Create routing select per output",
          "enable_button": "Create 'next source' button per output",
          "enable_link_sensors": "Create HDMI link sensors"
        }
      }
    }
  }
}
